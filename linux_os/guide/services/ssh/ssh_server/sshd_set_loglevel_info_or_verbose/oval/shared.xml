{{% set sshpath="/etc/ssh/sshd_config" %}}
{{% set sshparameter="LogLevel" %}}
<def-group>
  <definition class="compliance" id="{{{ rule_id }}}" version="1">
    {{{ oval_metadata("The SSH LogLevel should be set to INFO or VERBOSE") }}}
    <criteria comment="SSH is configured correctly or is not installed"
    operator="OR">
      <criteria comment="sshd is not installed" operator="AND">
        <extend_definition comment="sshd is not required or requirement is unset"
        definition_ref="sshd_not_required_or_unset" />
        {{% if product in ['opensuse', 'sle12'] %}}
        <extend_definition comment="rpm package openssh removed"
        definition_ref="package_openssh_removed" />
        {{% else %}}
        <extend_definition comment="rpm package openssh-server removed"
        definition_ref="package_openssh-server_removed" />
        {{% endif %}}
      </criteria>
      <criteria comment="sshd is installed and configured" operator="AND">
        <extend_definition comment="sshd is required or requirement is unset"
        definition_ref="sshd_required_or_unset" />
        {{% if product in ['opensuse', 'sle12'] %}}
        <extend_definition comment="rpm package openssh installed"
        definition_ref="package_openssh_installed" />
        {{% else %}}
        <extend_definition comment="rpm package openssh-server installed"
        definition_ref="package_openssh-server_installed" />
        {{% endif %}}
        {{{- oval_line_in_file_criterion(sshpath, sshparameter) }}}
      </criteria>
    </criteria>
  </definition>
  {{{ oval_line_in_file_test(sshpath, sshparameter) }}}
  {{{ oval_line_in_file_object(path=sshpath, prefix_regex="^[ \\t]*(?i)", parameter=sshparameter, separator_regex='[\s]+') }}}
  {{{ oval_line_in_file_state(value='(INFO|VERBOSE)') }}}
</def-group>
