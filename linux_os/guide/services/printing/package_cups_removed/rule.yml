documentation_complete: true

prodtype: ubuntu2004

title: 'Uninstall cups Package'

description: |-
    {{{ describe_package_remove(package="cups") }}}

rationale: |-
    If the system does not need to print jobs or accept print jobs from other
    systems, it is recommended that CUPS be removed to reduce the potential
    attack surface.

severity: low

references:
    cis@ubuntu2004: 2.2.4

ocil: |-
    If network services are using the cups service, this is not
    applicable.
    <br /><br />
    {{{ complete_ocil_entry_package(package="cups") }}}

template:
    name: package_removed
    vars:
        pkgname: cups
