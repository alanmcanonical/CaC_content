# platform = multi_platform_all
# reboot = false
# strategy = restrict
# complexity = low
# disruption = low
{{{
    ansible_exim4_config_set(
        parameter=PARAMETER,
        value=VALUE
    )
}}}
